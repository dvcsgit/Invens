
@model PFG.Inventory.Web.ViewModels.InventoryListViewModels
@{
    Layout = null;
}
@using (Ajax.BeginForm("Query", null, SiteLibrary.GetDefaultAjaxOptions(), new { @id = "criteria-form" }))
{
    @Html.HiddenEntirelyFor(x => x.Parameters)

}