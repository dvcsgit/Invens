@model PFG.Inventory.Web.ViewModels.InventoryStockListViewModels
@{
    Layout = null;
}
@using (Ajax.BeginForm("Index", null, SiteLibrary.GetDefaultAjaxOptions(), new { @id = "criteria-form" }))
{
    @Html.HiddenEntirelyFor(x => x.Parameters)

}
